<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TimeScale"
             x:Class="TimeScale.MainPage" BackgroundColor="DarkMagenta">

    <StackLayout>
        <StackLayout VerticalOptions="EndAndExpand" Padding="20" BackgroundColor="#646864">
            <Button Text="Novo Jogador" Style="{StaticResource ButtonMainPage}" BackgroundColor="{StaticResource buttonPlayer_color}" Command="{Binding PlayerCommand}"/>
            <Button Text="Escala Time" Style="{StaticResource ButtonMainPage}" BackgroundColor="{StaticResource buttonScale_color}"  Command="{Binding ScaleCommand}"/>            
        </StackLayout>
    </StackLayout>

</ContentPage>