<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:TimeScale"
             x:Class="TimeScale.MainPage">

    <Grid Padding="2" BackgroundColor="#646864">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Text="Novo Jogador" Font="20" BackgroundColor="{StaticResource buttonPlayer_color}" Command="{Binding PlayerCommand}"/>
        <Button Grid.Column="1" Text="Escala Time" Font="20" BackgroundColor="{StaticResource buttonScale_color}"/>        
        
    </Grid>

</ContentPage>